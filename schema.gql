# Flat GraphQL schema.
schema {
    query: Query
}

type Query {
    hello: String!
    user: User!
    users: [User!]!
    message: Message!
}

type User {
    name: String!
    mail: String!
}

type Message {
    text: String!
    from: User!
    to: [User!]!
}